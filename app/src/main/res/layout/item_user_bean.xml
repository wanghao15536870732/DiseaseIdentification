<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="4dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_item_account"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical|start"
        android:layout_marginEnd="10dp"
        android:layout_toStartOf="@+id/iv_item_head_icon"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="10dp"
        android:layout_toLeftOf="@+id/iv_item_head_icon"
        android:text="123456789"
        style="@style/AccountTextStyle"
        />

    <ImageView
        android:id="@+id/iv_item_head_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_toStartOf="@id/iv_item_clear_account"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:layout_toLeftOf="@id/iv_item_clear_account"
        android:layout_marginRight="10dp"
        android:src="@mipmap/clear"
        />

    <ImageView
        android:id="@+id/iv_item_clear_account"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:src="@mipmap/clear"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp" />

</RelativeLayout>